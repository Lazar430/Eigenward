---
interface Props {
  title: string;
  text: string;
  items: string[];
}

const { title, text, items } = Astro.props;
---

<article class="branch-card">
  <h3>{title}</h3>
  <p>{text}</p>

  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</article>