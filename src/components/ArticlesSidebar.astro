---
import { withBase } from "@/utils/paths";

const items = [
  { label: "Algebra", href: withBase("articles/") },
  { label: "Analysis", href: withBase("articles/") },
  { label: "Number Theory", href: withBase("articles/") },
  { label: "Probability", href: withBase("articles/") }
];
---

<aside class="hub-sidebar" aria-label="Articles navigation">
  <span class="hub-sidebar-title">Articles</span>

  <nav>
    {items.map((item) => <a href={item.href}>{item.label}</a>)}
  </nav>
</aside>
